/**
 * The "Get a demo" booking URL used by every <DemoCTA /> on the site.
 *
 * Update this when the operator's preferred booking platform changes
 * (Cal.com, Calendly, a hosted form, etc.). Single source of truth so
 * the hero, the closing CTA, and the nav button never disagree.
 */
export const demoBookingUrl = 'https://cal.com/scanipy/demo'
